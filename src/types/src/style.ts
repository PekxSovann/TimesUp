export interface TextStyle {

};

export interface ButtonGradientStyle {
  buttonWidth: number;
  buttonHeight: number;
  borderRadius: number;
  borderColor: string;
  borderWidth: number;
};

export interface GradientStyle {
  borderRadius: number;
  justifyContent: string;
  alignItems: string;
};